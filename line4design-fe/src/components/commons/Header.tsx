/** @format */

import React from 'react';

interface Props {}

const Header = (props: Props) => {
  return (
    <div className="header_container">
      <div>Logo</div>
    </div>
  );
};

export default Header;
